---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-client
  namespace: tekkom-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-client
  template:
    metadata:
      labels:
        app: postgres-client
    spec:
      containers:
        - name: postgres-client
          image: debian:trixie
          env:
            - name: PGHOST
              value: tekkom-bot-api-psql-rw.tekkom-bot.svc.cluster.local
            - name: PGPORT
              value: "5432"
            - name: PGDATABASE
              value: tekkom-bot
            - name: PGUSER
              value: tekkom-bot
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: tekkom-bot-secrets
                  key: DB_PASSWORD
          command: ["/bin/sh"]
          args:
            [
              "-c",
              "apt-get update && apt-get install -y vim postgresql-client && sleep infinity",
            ]
