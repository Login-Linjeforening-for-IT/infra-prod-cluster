---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workerbee
  namespace: workerbee
spec:
  ingressClassName: nginx-ingress-external
  tls:
    - hosts:
        - workerbee.login.no
  rules:
    - host: workerbee.login.no
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: workerbee
                port:
                  number: 8080
