---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: beehive-backend-ingress
  namespace: beehive
spec:
  ingressClassName: nginx-external
  tls:
    - hosts:
        - workerbee.login.no
  rules:
    - host: workerbee.login.no
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: beehive-backend
                port:
                  number: 8080
