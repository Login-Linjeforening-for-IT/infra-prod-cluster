# Denies everything except policies
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all
  namespace: scouterbee
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress

# Allows Kube DNS traffic
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-kube-dns
  namespace: scouterbee
spec:
  podSelector:
    matchLabels:
      app: scouterbee
  policyTypes:
    - Ingress
    - Egress
  egress:
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53

# Allowing egress scouterbee to internet (required for talking to Gitlab)
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-scouterbee-egress-internet
  namespace: scouterbee
spec:
  podSelector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - scouterbee
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0

# Allowing egress bot to uptime-kuma traffic
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-bot-to-uptime-kuma
  namespace: scouterbee
spec:
  podSelector:
    matchLabels:
      app: scouterbee
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: uptime-kuma
          podSelector:
            matchLabels:
              app: uptime-kuma
      ports:
        - protocol: TCP
          port: 3001
