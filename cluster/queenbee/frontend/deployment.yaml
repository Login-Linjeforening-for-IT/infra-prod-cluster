---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: queenbee-frontend
  name: queenbee-frontend
  namespace: queenbee
spec:
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  selector:
    matchLabels:
      app: queenbee-frontend
  template:
    metadata:
      labels:
        app: queenbee-frontend
    spec:
      imagePullSecrets:
        - name: gitlab-frontend-pull-token
      containers:
        - image: registry.login.no/tekkom/web/beehive/queenbee:1.3.26@sha256:3a69f97f0c3e567f20a10eb800596cf0bc341a160cca8687d38322501d358a6b
          imagePullPolicy: Always
          name: queenbee-frontend
          env:
            - name: BASE_URL
              value: https://queenbee-api.login.no/v1
          ports:
            - containerPort: 4200
              name: 4200tcp
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: 4200
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 4200
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - queenbee-frontend
                topologyKey: kubernetes.io/hostname
